mutation CreateOrderWithoutUser(
  $orderTotal: Float!
  $orderValue: Float!
  $stripeCheckoutId: String!
  $paymentType: String
  $parcel: Int
  $items: [OrderItemCreateInput!]!
) {
  createOrder(
    data: {
      total: $orderTotal
      orderValue: $orderValue
      stripeCheckoutId: $stripeCheckoutId
      parcel: $parcel
      paymentType: $paymentType
      orderItems: { create: $items }
    }
  ) {
    id
    orderItems {
      id
    }
  }
}
