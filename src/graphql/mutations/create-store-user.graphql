mutation CreateStoreUser(
  $name: String!
  $surname: String!
  $description: String
  $email: String
  $nickname: String
) {
  createStoreUser(
    data: {
      name: $name
      surname: $surname
      email: $email
      description: $description
      nickname: $nickname
    }
  ) {
    id
  }
}
