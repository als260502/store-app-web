mutation CreateProduct(
  $name: String!
  $slug: String!
  $description: String!
  $price: Float!
  $quantity: Int!
) {
  createProduct(
    data: {
      name: $name
      slug: $slug
      description: $description
      price: $price
      quantity: $quantity
    }
  ) {
    id
    slug
  }
}
