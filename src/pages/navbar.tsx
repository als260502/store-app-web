import { NextPage } from "next";
import { NavBar as Nav } from "../components/Sidebar/NavBar";

const NavBar: NextPage = () => {
  return (
    <div>
      <Nav />
    </div>
  );
};

export default NavBar;
